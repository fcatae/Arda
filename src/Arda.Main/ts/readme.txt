Visual Studio uses old typescript version. The following configuration was
added to .csproj file to prevent typescript compilation.

    <TypeScriptCompileBlocked>true</TypeScriptCompileBlocked>

Thus, typescript files must be manually compiled.
    